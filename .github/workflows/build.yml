name: Build GWBHJ

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - abi: arm64-v8a
            target: aarch64-linux-android
            ndk_target: arm64-v8a
          - abi: armeabi-v7a
            target: armv7a-linux-androideabi
            ndk_target: armeabi-v7a
          - abi: x86_64
            target: x86_64-linux-android
            ndk_target: x86_64
          - abi: x86
            target: i686-linux-android
            ndk_target: x86

    steps:
      - uses: actions/checkout@v4

      - name: Setup NDK
        uses: nttld/setup-ndk@v1
        id: setup-ndk
        with:
          ndk-version: r27c
          add-to-path: true

      - name: Build ${{ matrix.abi }}
        run: |
          NDK=${{ steps.setup-ndk.outputs.ndk-path }}
          TOOLCHAIN=$NDK/toolchains/llvm/prebuilt/linux-x86_64
          API=21

          mkdir -p .build/cmake-${{ matrix.abi }}
          cd .build/cmake-${{ matrix.abi }}

          cmake -S ${{ github.workspace }}/src -B . \
            -DCMAKE_TOOLCHAIN_FILE=$NDK/build/cmake/android.toolchain.cmake \
            -DANDROID_ABI=${{ matrix.abi }} \
            -DANDROID_PLATFORM=android-$API \
            -DCMAKE_BUILD_TYPE=MinSizeRel

          cmake --build . -j$(nproc)

          mkdir -p ${{ github.workspace }}/.build/stage/zygisk
          cp libspoof.so ${{ github.workspace }}/.build/stage/zygisk/${{ matrix.abi }}.so
          $TOOLCHAIN/bin/llvm-strip --strip-unneeded ${{ github.workspace }}/.build/stage/zygisk/${{ matrix.abi }}.so

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.abi }}
          path: .build/stage/zygisk/${{ matrix.abi }}.so

  package:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Download all ABIs
        uses: actions/download-artifact@v4
        with:
          path: .build/abis

      - name: Package module
        run: |
          VERSION="1.0.15"
          VERSION_CODE="115"
          STAGE=".build/stage"

          mkdir -p "$STAGE/zygisk"

          cp -r module/. "$STAGE/"

          for abi in arm64-v8a armeabi-v7a x86_64 x86; do
            cp ".build/abis/$abi/$abi.so" "$STAGE/zygisk/"
          done

          cat > "$STAGE/module.prop" <<EOF
          id=gwbhj_jailbreak
          name=过强标黑机
          version=$VERSION
          versionCode=$VERSION_CODE
          author=蜡笔不小心
          description=过腾讯黑名单，采用白名单控制默认添加三角洲，其它软件去"whitelist.txt"添加，自带越狱免解过环境，支持冻结精简多开系统软件。
          minMagisk=20.4
          EOF

          chmod 0755 "$STAGE/customize.sh" "$STAGE/service.sh" "$STAGE/uninstall.sh" \
                     "$STAGE/action.sh" \
                     "$STAGE/META-INF/com/google/android/update-binary"
          chmod 0644 "$STAGE/whitelist.txt"

          cd "$STAGE"
          zip -r -q -X "$GITHUB_WORKSPACE/GWBHJ-v${VERSION}.zip" . -x '.*'

      - name: Upload ZIP
        uses: actions/upload-artifact@v4
        with:
          name: GWBHJ-module
          path: GWBHJ-v1.0.0.zip
